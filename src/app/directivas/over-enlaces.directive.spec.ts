import { OverEnlacesDirective } from './over-enlaces.directive';

describe('OverEnlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new OverEnlacesDirective();
    expect(directive).toBeTruthy();
  });
});
